//------------------------random value function-------------------------------------
export default function random_value_arrange(max){
	return Math.floor(Math.random() * max)
}
//----------------------------------------------------------------------------------
